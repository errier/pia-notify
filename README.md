# WebExtension

This WebExtension (Chrome extension/Firefox Add-on) will scrape https://www.privateinternetaccess.com to determine if your're protected by PIA or not.  
If not, it will temporarily show a warning on top of the page.
